func floor(num){
    return num - (num % 1);
}

func ceil(num){
    return (num + 1) - (num % 1);
}

func round(num){
    if num % 1 > 0.5 {
        return ceil(num);
    }
    return floor(num);
}



extract floor;
extract ceil;
extract round;