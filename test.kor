func NewUser(name,age) {
  var re = {
    name : name,
    age : age
  };
  return re;
}

var users = [ NewUser("Ahmet",21), NewUser("Kenan",15) ];

for var i = 0; i < len(users); i++; {
  var user = users[i];
  print(user["name"]);
}